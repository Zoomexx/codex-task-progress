# Changelog

## v0.4.2 — 2026-09-09

- 修复长会话日志的处理时长误把会话创建时间/历史累计时间当作当前任务时长。
- 任务边界不在日志尾部时，以终止事件后的首个新工作或用户事件作为当前任务起点；冷启动不再把 session metadata 时间当作任务开始。
- 保留长日志增量读取、任务状态纠偏、并行任务隔离 token、深链、无滚动布局和单实例保护。

## v0.4.1 — 2026-09-09

- 在任务面板底部留白增加低干扰横向运行状态动画，并以最后一行实际下缘为基准居中。
- 修复任务每 4 秒刷新或窗口布局时重置 Core Animation 导致的断续闪烁；普通刷新会复用现有动画。
- 增加 macOS“减少动态效果”支持，减少动态效果开启时显示静态提示。
- README 按任务、交互、详情、token、用量、动画、可靠性和隐私分项说明全部功能。

## v0.4.0 — 2026-09-08

- 使用 `codex://threads/<session_id>` 深链打开对应 Codex 对话。
- 从全部本地会话日志读取最新累计 token 快照，按日志文件去重后汇总。
- 保留进行中 / 已完成双栏、更多任务独立窗口、悬停透明度和鼠标移出自动隐藏。
- 任务详情显示已处理时间、本轮 token、当前步骤和待授权状态。
- 官方剩余用量改为 App Server 只读接口和更新事件同步。
- 主面板改为无边框窗口，统一自绘圆角边框，修复底部边框不一致。
- 增加解析器冒烟测试和可指定临时输出目录的构建脚本。
